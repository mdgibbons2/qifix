# Editors / OS
.DS_Store
*.log

# Secrets / env
.env
.env.*
*.key
*.keys.json
secrets.json
credentials.json

# Node
node_modules/

# ----- Private / not-in-homepage-repo -----
# The homepage repo only ships /homepage (minus /homepage/security) + /images + /screenshots.
# Everything below lives locally or in a separate (private) repo.

# Electron app - lives in its own private qifix-client repo
app/

# PRDs / internal docs
docs/

# Scratch files
/images/Misc Whiteboard*.png
screenshots/

# Security / compliance docs - kept local, not published yet
homepage/security/

# Generated launcher .app (local, user-specific paths inside)
qifix.app/
